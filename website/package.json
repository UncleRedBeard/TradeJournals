{
  "name": "time-and-timber-restoration",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": "24.21.0"
  },
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/*.test.mjs",
    "build": "node scripts/build.mjs release",
    "build:preview": "node scripts/build.mjs preview",
    "check:output": "node scripts/check-output.mjs .preview-dist"
  },
  "dependencies": {
    "astro": "7.3.2",
    "zod": "4.5.4"
  }
}
